Replace the current procedural Winds Aloft generation system with real forecast-model winds.

Current behavior:

* Winds aloft are generated from the surface METAR wind using estimated direction offsets and speed increments.
* FL100, FL240 and FL360 winds are currently synthetic and not based on real atmospheric data.

Required change:

* Remove the procedural wind generation logic entirely.
* Fetch real winds aloft from the Open-Meteo forecast API using the aircraft's current latitude and longitude from SimConnect.
* Keep all modifications inside MainWindow.xaml.cs.
* Do not create any new source files.

Use forecast pressure levels:

700 hPa -> FL100
500 hPa -> FL240
250 hPa -> FL360

Retrieve:

wind_speed_700hPa
wind_direction_700hPa

wind_speed_500hPa
wind_direction_500hPa

wind_speed_250hPa
wind_direction_250hPa

Implementation requirements:

1. Add an internal winds-aloft cache structure inside MainWindow.xaml.cs to store:

   * FL100 direction/speed
   * FL240 direction/speed
   * FL360 direction/speed

2. Add an asynchronous method that downloads the above forecast values from Open-Meteo using aircraft position.

3. Use PositionData.Latitude and PositionData.Longitude as the forecast lookup coordinates.

4. Refresh forecast winds only once every 15 minutes.

   * Store a lastWindsUpdate timestamp.
   * Avoid downloading on every SimConnect update.

5. In Simconnect_OnRecvSimobjectData():

   * Check whether winds data is older than 15 minutes.
   * Trigger an asynchronous refresh when needed.
   * Do not block the UI thread.

6. In ParseAndSanitizeMetar():

   * Remove all procedural FL100/FL240/FL360 wind calculations.
   * Replace them with the downloaded forecast values.

7. Continue generating P3D upper-air layers exactly as before.

   * Only the wind source changes.
   * The WeatherSetObservation workflow remains unchanged.

8. Update the Winds Aloft UI panel so:

   * FL100 displays 700 hPa winds.
   * FL240 displays 500 hPa winds.
   * FL360 displays 250 hPa winds.

9. Preserve:

   * METAR interpolation
   * ATIS generation
   * cloud processing
   * visibility logic
   * inversion layer logic
   * weather injection architecture
   * UI layout

The sole objective is to replace guessed winds aloft with real forecast-model winds while keeping the rest of the engine unchanged.
